package org.example;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        System.out.println("hello world!");

//        GameController controller = new GameController();
//
//        //.equals() check compares values of 2 objects
//        while(controller.getGameStatus(game).equals(IN_PROGRESS)){
//            //ask the player to play a move
//            controller.makeMove(game);
//            controller.displayBoard(game);
//            controller.checkWinner(game);
//        }
    }
}